<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loteriaapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.LoteriaApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".views.CreateApuestaActivity"></activity>
        <activity android:name=".views.RegisterActivity" />
        <activity
            android:name=".views.ApuestasActivity"
            android:label="@string/title_activity_apuestas"
            android:theme="@style/Theme.LoteriaApp.NoActionBar" />
        <activity
            android:name=".views.HomeActivity"
            android:theme="@style/Theme.LoteriaApp.NoActionBar" />
        <activity
            android:name=".views.LoginActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>